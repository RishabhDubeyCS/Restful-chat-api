<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>New Chat | Mini-WhatsApp</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@400;600;700&display=swap" rel="stylesheet">
    
    <style>
        :root {
            --bg-dark: #0f1012;
            --card-gray: #1c1e21;
            --wa-green: #25D366;
            --input-focus: #2a2d31;
        }

        body {
            background-color: var(--bg-dark);
            font-family: 'Plus Jakarta Sans', sans-serif;
            color: #ffffff;
            height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0;
        }

        /* Fancy Form Card */
        .form-container {
            background-color: var(--card-gray);
            border: 1px solid #333;
            padding: 40px;
            border-radius: 30px;
            width: 100%;
            max-width: 500px;
            box-shadow: 0 20px 50px rgba(0,0,0,0.3);
        }

        h3 {
            font-weight: 700;
            margin-bottom: 25px;
            background: linear-gradient(to right, #fff, var(--wa-green));
            -webkit-background-clip: text;
            -webkit-fill-color: transparent;
            background-clip: text;
            color: transparent;
        }

        /* Modern Input Styling */
        .form-label {
            font-size: 0.85rem;
            color: #888;
            margin-bottom: 8px;
            font-weight: 600;
        }

        .custom-input {
            background-color: #121212;
            border: 1px solid #333;
            border-radius: 12px;
            color: white;
            padding: 12px 15px;
            width: 100%;
            margin-bottom: 20px;
            transition: all 0.3s ease;
        }

        .custom-input:focus {
            outline: none;
            border-color: var(--wa-green);
            background-color: var(--input-focus);
            box-shadow: 0 0 10px rgba(37, 211, 102, 0.2);
        }

        textarea.custom-input {
            min-height: 120px;
            resize: none;
        }

        /* Fancy Button */
        .btn-create {
            background: var(--wa-green);
            color: #000;
            font-weight: 700;
            border: none;
            padding: 14px;
            border-radius: 12px;
            width: 100%;
            transition: all 0.3s;
            text-transform: uppercase;
            letter-spacing: 1px;
        }

        .btn-create:hover {
            transform: translateY(-3px);
            box-shadow: 0 10px 20px rgba(37, 211, 102, 0.3);
            background-color: #21bd5b;
        }

        .back-link {
            display: block;
            text-align: center;
            margin-top: 20px;
            color: #666;
            text-decoration: none;
            font-size: 0.9rem;
        }

        .back-link:hover {
            color: var(--wa-green);
        }
    </style>
</head>
<body>

    <div class="container d-flex justify-content-center px-3">
        <div class="form-container">
            <h3>Write new chat...</h3>

            <form action="/chats" method="POST">
                
                <div class="mb-1">
                    <label class="form-label">Sender Name</label>
                    <input type="text" placeholder="Who are you?" name="from" class="custom-input" required />
                </div>

                <div class="mb-1">
                    <label class="form-label">Recipient Name</label>
                    <input type="text" placeholder="Who are you messaging?" name="to" class="custom-input" required />
                </div>

                <div class="mb-1">
                    <label class="form-label">Your Message</label>
                    <textarea name="msg" class="custom-input" placeholder="Type a message..."></textarea>
                </div>

                <button type="submit" class="btn-create">Create Chat</button>
                
                <a href="/chats" class="back-link">← Cancel and go back</a>
            </form>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>