<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>RESTful API Portfolio | Mini-WhatsApp</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">
    <link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@400;600;800&family=Fira+Code:wght@400;500&display=swap" rel="stylesheet">
    
    <style>
        :root {
            --bg-dark: #08090a;
            --wa-green: #25D366;
            --wa-teal: #128C7E;
            --card-glass: rgba(255, 255, 255, 0.03);
            --border-glass: rgba(255, 255, 255, 0.1);
        }

        body {
            background-color: var(--bg-dark);
            font-family: 'Plus Jakarta Sans', sans-serif;
            color: #ffffff;
            overflow-x: hidden;
        }

        /* Animated Background Blobs */
        .blob {
            position: absolute;
            width: 400px;
            height: 400px;
            background: linear-gradient(135deg, var(--wa-green), var(--wa-teal));
            filter: blur(80px);
            border-radius: 50%;
            z-index: -1;
            opacity: 0.15;
            animation: move 20s infinite alternate;
        }

        @keyframes move {
            from { transform: translate(-10%, -10%); }
            to { transform: translate(50%, 20%); }
        }

        /* Navbar */
        .navbar {
            backdrop-filter: blur(15px);
            border-bottom: 1px solid var(--border-glass);
        }

        /* Hero */
        .hero {
            padding: 140px 0 80px 0;
        }

        .hero-title {
            font-size: clamp(2.5rem, 6vw, 4.5rem);
            font-weight: 800;
            background: linear-gradient(to bottom right, #fff 30%, #666);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            letter-spacing: -2px;
        }

        .stack-badge {
            background: var(--card-glass);
            border: 1px solid var(--border-glass);
            padding: 6px 16px;
            border-radius: 50px;
            font-size: 0.8rem;
            color: #aaa;
            margin: 5px;
            display: inline-block;
        }

        /* CRUD Cards */
        .crud-card {
            background: var(--card-glass);
            border: 1px solid var(--border-glass);
            backdrop-filter: blur(10px);
            border-radius: 24px;
            padding: 30px;
            transition: all 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);
        }

        .crud-card:hover {
            border-color: var(--wa-green);
            transform: translateY(-10px);
            box-shadow: 0 20px 40px rgba(37, 211, 102, 0.1);
        }

        /* REST Table */
        .api-table {
            background: var(--card-glass);
            border-radius: 20px;
            overflow: hidden;
            border: 1px solid var(--border-glass);
        }

        .api-table th { background: rgba(255,255,255,0.05); color: #888; border: none; }
        .api-table td { border-top: 1px solid var(--border-glass); vertical-align: middle; }

        .method-tag {
            font-family: 'Fira Code', monospace;
            font-size: 0.75rem;
            padding: 4px 10px;
            border-radius: 6px;
            font-weight: bold;
        }

        .get { color: #61afef; background: rgba(97, 175, 239, 0.1); }
        .post { color: #98c379; background: rgba(152, 195, 121, 0.1); }
        .patch { color: #e5c07b; background: rgba(229, 192, 123, 0.1); }
        .delete { color: #e06c75; background: rgba(224, 108, 117, 0.1); }

        .btn-primary-wa {
            background: var(--wa-green);
            color: #000;
            font-weight: 700;
            padding: 14px 32px;
            border-radius: 14px;
            border: none;
            transition: 0.3s;
        }

        .btn-primary-wa:hover {
            background: #fff;
            transform: scale(1.05);
        }

        .code-window {
            background: #1a1c1e;
            border-radius: 20px;
            border: 1px solid var(--border-glass);
            box-shadow: 0 30px 60px rgba(0,0,0,0.5);
        }
    </style>
</head>
<body>

    <div class="blob"></div>

    <nav class="navbar navbar-expand-lg navbar-dark fixed-top">
        <div class="container">
            <a class="navbar-brand fw-bold" href="#">
                <i class="bi bi-braces text-success me-2"></i>DEV_PROJECT
            </a>
            <div class="d-flex gap-3">
                <a href="/chats" class="btn btn-sm btn-outline-light rounded-pill px-3">Live Dashboard</a>
            </div>
        </div>
    </nav>

    <header class="hero text-center">
        <div class="container">
            <div class="mb-4">
                <span class="stack-badge"><i class="bi bi-nodejs text-success me-2"></i>Node.js</span>
                <span class="stack-badge"><i class="bi bi-database text-info me-2"></i>MongoDB</span>
                <span class="stack-badge"><i class="bi bi-server text-warning me-2"></i>Express</span>
            </div>
            <h1 class="hero-title">Full-Stack RESTful <br> Message Engine.</h1>
            <p class="lead text-secondary mt-4 mb-5 mx-auto" style="max-width: 650px;">
                A technical showcase of <b>CRUD architecture</b>, asynchronous database operations, 
                and server-side rendering using the EJS engine.
            </p>
            <div class="d-flex justify-content-center gap-3">
                <a href="/chats" class="btn-primary-wa text-decoration-none">Explore API</a>
                <a href="https://github.com" class="btn btn-outline-light px-4 py-3 rounded-4 border-secondary">
                    <i class="bi bi-github me-2"></i>Source Code
                </a>
            </div>
        </div>
    </header>

    <section class="container py-5">
        <div class="row g-4">
            <div class="col-md-4">
                <div class="crud-card">
                    <i class="bi bi-cloud-arrow-down text-success fs-2"></i>
                    <h4 class="mt-3">Data Retrieval</h4>
                    <p class="text-secondary small">Optimized Mongoose queries to fetch documents with real-time timestamps.</p>
                </div>
            </div>
            <div class="col-md-4">
                <div class="crud-card">
                    <i class="bi bi-pencil-square text-success fs-2"></i>
                    <h4 class="mt-3">State Management</h4>
                    <p class="text-secondary small">Handling PATCH and DELETE requests using method-override middleware.</p>
                </div>
            </div>
            <div class="col-md-4">
                <div class="crud-card">
                    <i class="bi bi-shield-check text-success fs-2"></i>
                    <h4 class="mt-3">Input Validation</h4>
                    <p class="text-secondary small">Server-side logic to ensure data integrity before persisting to MongoDB.</p>
                </div>
            </div>
        </div>
    </section>

    <section class="container py-5">
        <h3 class="fw-bold mb-4 text-center">API Documentation</h3>
        <div class="api-table p-2">
            <table class="table table-dark table-hover mb-0">
                <thead>
                    <tr>
                        <th>Method</th>
                        <th>Endpoint</th>
                        <th>Functionality</th>
                    </tr>
                </thead>
                <tbody>
                    <tr>
                        <td><span class="method-tag get">GET</span></td>
                        <td><code>/chats</code></td>
                        <td class="text-secondary">List all messages (Index)</td>
                    </tr>
                    <tr>
                        <td><span class="method-tag post">POST</span></td>
                        <td><code>/chats</code></td>
                        <td class="text-secondary">Initialize new message (Create)</td>
                    </tr>
                    <tr>
                        <td><span class="method-tag patch">PATCH</span></td>
                        <td><code>/chats/:id</code></td>
                        <td class="text-secondary">Partial document update (Update)</td>
                    </tr>
                    <tr>
                        <td><span class="method-tag delete">DELETE</span></td>
                        <td><code>/chats/:id</code></td>
                        <td class="text-secondary">Resource destruction (Delete)</td>
                    </tr>
                </tbody>
            </table>
        </div>
    </section>

    <footer class="text-center py-5">
        <hr class="border-secondary opacity-25">
        <p class="text-secondary small mt-4">Handcrafted by Rishabh Dubey | Full Stack Developer Candidate</p>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>