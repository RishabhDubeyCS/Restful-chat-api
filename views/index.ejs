<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mini-WhatsApp | Light Edition</title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">
    <link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@400;600;700;800&display=swap" rel="stylesheet">

    <style>
        :root {
            --bg-white: #ffffff;
            --card-white: #f8f9fa;
            --accent-yellow: #FFD700; /* Gold Yellow */
            --text-black: #000000;
            --text-gray: #4a4a4a;
            --border-light: #e9ecef;
        }

        body {
            background-color: var(--bg-white);
            font-family: 'Plus Jakarta Sans', sans-serif;
            color: var(--text-black);
            overflow-x: hidden;
        }

        /* Nav Bar / Top Actions */
        .top-nav {
            padding: 1.5rem 1rem 0 1rem;
            display: flex;
            justify-content: flex-start;
        }

        .btn-home {
            color: var(--text-black);
            text-decoration: none;
            font-weight: 700;
            font-size: 0.9rem;
            display: flex;
            align-items: center;
            gap: 8px;
            transition: 0.3s;
            padding: 8px 16px;
            border-radius: 50px;
            border: 1px solid transparent;
        }

        .btn-home:hover {
            background-color: var(--card-white);
            border-color: var(--accent-yellow);
        }

        /* Responsive Header Section */
        .header-section {
            padding: clamp(2rem, 6vh, 4rem) 1rem;
            background: linear-gradient(180deg, rgba(255, 215, 0, 0.2) 0%, var(--bg-white) 100%);
            border-bottom: 1px solid var(--border-light);
            margin-bottom: clamp(1.5rem, 5vh, 3rem);
        }

        .hero-title {
            font-size: clamp(2rem, 6vw, 3.8rem);
            font-weight: 800;
            color: var(--text-black);
            letter-spacing: -1.5px;
        }

        /* High-Contrast Cards */
        .chat-card {
            background-color: var(--card-white);
            border: 1px solid var(--border-light);
            border-radius: 28px;
            transition: all 0.4s cubic-bezier(0.165, 0.84, 0.44, 1);
            height: 100%;
            display: flex;
            flex-direction: column;
            padding: 24px;
        }

        .chat-card:hover {
            transform: translateY(-10px);
            background-color: #ffffff;
            border-color: var(--accent-yellow);
            box-shadow: 0 20px 40px rgba(0, 0, 0, 0.08);
        }

        .avatar {
            width: 52px;
            height: 52px;
            background: var(--text-black);
            color: var(--accent-yellow);
            display: flex;
            align-items: center;
            justify-content: center;
            border-radius: 16px;
            font-weight: 800;
            font-size: 1.2rem;
            flex-shrink: 0;
        }

        .message-box {
            background-color: #ffffff;
            border-radius: 20px;
            padding: 1.25rem;
            margin: 1.25rem 0;
            border: 1px solid var(--border-light);
            border-left: 5px solid var(--accent-yellow);
            flex-grow: 1;
        }

        .msg-text { color: var(--text-gray); font-size: 1rem; line-height: 1.6; }

        .to-text {
            color: var(--text-black);
            font-weight: 700;
            font-size: 0.8rem;
            background: var(--accent-yellow);
            padding: 2px 10px;
            border-radius: 50px;
        }

        .timestamp {
            font-size: 0.75rem;
            color: #999;
            margin-bottom: 1.25rem;
            display: flex;
            align-items: center;
            gap: 6px;
        }

        .btn-custom {
            border-radius: 14px;
            padding: 8px 20px;
            font-size: 0.85rem;
            font-weight: 700;
            transition: 0.3s;
            border: 2px solid var(--text-black);
        }

        .btn-edit { background: var(--accent-yellow); color: var(--text-black); }
        .btn-edit:hover { background: var(--text-black); color: var(--accent-yellow); }

        .btn-delete { border-color: transparent; color: #dc3545; background: rgba(220, 53, 69, 0.05); }
        .btn-delete:hover { background: #dc3545; color: #fff; }

        .btn-new {
            background-color: var(--text-black);
            color: var(--accent-yellow);
            border: none;
            padding: 12px 35px;
            border-radius: 50px;
            font-weight: 800;
            box-shadow: 0 10px 20px rgba(0,0,0,0.15);
        }

        .btn-new:hover { transform: translateY(-3px); background-color: #333; }

        .fab {
            position: fixed; bottom: 30px; right: 30px; width: 64px; height: 64px;
            background: var(--text-black); border-radius: 20px; display: flex;
            align-items: center; justify-content: center; color: var(--accent-yellow);
            font-size: 28px; box-shadow: 0 15px 30px rgba(0,0,0,0.2);
            text-decoration: none; transition: 0.3s; z-index: 1000;
        }
        
        .fab:hover { transform: scale(1.1); }

        @media (max-width: 576px) {
            .hero-btns .btn-new { width: 100%; }
        }
    </style>
</head>

<body>

<div class="top-nav">
    <a href="/" class="btn-home">
        <i class="bi bi-arrow-left-circle-fill"></i>
        Back to Home
    </a>
</div>

<header class="header-section text-center">
    <div class="container">
        <h1 class="hero-title">Mini-WhatsApp <span style="text-decoration: underline var(--accent-yellow) 6px;">Studio</span></h1>
        <p class="text-muted fw-semibold small mt-3">Full-Stack RESTful API Showcase</p>
        <div class="mt-4 hero-btns">
            <a href="/chats/new" class="btn btn-new">Start New Chat</a>
        </div>
    </div>
</header>

<main class="container pb-5">
    <div class="row g-4">
        <% for(let chat of chats) { %>
            <div class="col-12 col-md-6 col-lg-4">
                <div class="card chat-card shadow-sm">
                    <div class="d-flex align-items-center">
                        <div class="avatar me-3">
                            <%= chat.from.charAt(0).toUpperCase() %>
                        </div>
                        <div class="overflow-hidden">
                            <h6 class="mb-0 fw-bold"><%= chat.from %></h6>
                            <span class="to-text">TO: <%= chat.to %></span>
                        </div>
                    </div>

                    <div class="message-box">
                        <p class="mb-0 msg-text">"<%= chat.msg %>"</p>
                    </div>

                    <div class="timestamp">
                        <i class="bi bi-calendar3"></i>
                        <%= chat.created_at.toLocaleDateString('en-GB', { day: '2-digit', month: 'short' }) %> • 
                        <i class="bi bi-clock ms-1"></i>
                        <%= chat.created_at.toLocaleTimeString([], { hour: '2-digit', minute: '2-digit' }) %>
                    </div>

                    <div class="d-flex justify-content-between align-items-center mt-auto">
                        <div class="d-flex gap-2">
                            <a href="/chats/<%= chat._id %>/edit" class="btn-custom btn-edit text-decoration-none">Edit</a>
                            <form method="POST" action="/chats/<%= chat._id %>?_method=DELETE" class="m-0">
                                <button class="btn-custom btn-delete">Delete</button>
                            </form>
                        </div>
                        <i class="bi bi-check2-all fs-5" style="color: var(--accent-yellow);" title="Delivered"></i>
                    </div>
                </div>
            </div>
        <% } %>
    </div>
</main>

<a href="/chats/new" class="fab" title="New Chat"><i class="bi bi-plus-lg"></i></a>

<footer class="text-center py-5 border-top">
    <p class="small text-muted mb-0">Rishabh Dubey | Full Stack Developer Candidate | 2026</p>
</footer>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>