<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Message | CORE_NODE</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@400;600;700;800&display=swap" rel="stylesheet">
    
    <style>
        :root {
            --bg-dark: #0a0b1e;
            --bg-card: #16183d;
            --accent-primary: #7c4dff;
            --accent-secondary: #00e5ff;
            --text-main: #ffffff;
            --text-muted: #b0b3d6;
            --input-bg: rgba(0, 0, 0, 0.3);
            --border-light: rgba(255, 255, 255, 0.1);
        }

        @keyframes popIn {
            0% { opacity: 0; transform: scale(0.9) translateY(20px); }
            70% { transform: scale(1.02) translateY(-5px); }
            100% { opacity: 1; transform: scale(1) translateY(0); }
        }

        @keyframes fadeIn {
            from { opacity: 0; }
            to { opacity: 1; }
        }

        body {
            background-color: var(--bg-dark);
            font-family: 'Plus Jakarta Sans', sans-serif;
            color: var(--text-main);
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0;
            background: radial-gradient(circle at top right, rgba(124, 77, 255, 0.1), transparent 40%),
                        radial-gradient(circle at bottom left, rgba(0, 229, 255, 0.1), transparent 40%);
            animation: fadeIn 0.6s ease-out;
        }

        .form-container {
            background-color: var(--bg-card);
            border: 1px solid var(--border-light);
            padding: clamp(20px, 5vw, 40px);
            border-radius: 30px;
            width: 100%;
            max-width: 480px;
            box-shadow: 0 20px 50px rgba(0, 0, 0, 0.5);
            margin: 20px;
            animation: popIn 0.8s cubic-bezier(0.175, 0.885, 0.32, 1.275) both;
        }

        h3 {
            font-weight: 800;
            margin-bottom: 25px;
            text-transform: uppercase;
            letter-spacing: -1px;
            background: linear-gradient(135deg, #fff 30%, var(--accent-secondary) 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            display: inline-block;
            border-bottom: 3px solid var(--accent-primary);
            padding-bottom: 5px;
            transition: 0.3s;
        }

        .label-text {
            font-size: 0.8rem;
            color: var(--text-muted);
            text-transform: uppercase;
            letter-spacing: 1px;
            font-weight: 800;
            display: block;
            margin-bottom: 8px;
        }

        .input-readonly {
            background-color: rgba(0, 0, 0, 0.2) !important;
            border: 1px solid var(--border-light) !important;
            color: #666e99 !important;
            border-radius: 12px;
            padding: 12px 16px;
            width: 100%;
            margin-bottom: 20px;
            cursor: not-allowed;
            font-weight: 600;
            opacity: 0.8;
        }

        .custom-textarea {
            background-color: var(--input-bg);
            border: 1px solid var(--border-light);
            border-radius: 15px;
            color: #fff;
            padding: 15px;
            width: 100%;
            margin-bottom: 25px;
            transition: all 0.3s cubic-bezier(0.175, 0.885, 0.32, 1.275);
            resize: none;
            font-weight: 600;
        }

        .custom-textarea:focus {
            outline: none;
            border-color: var(--accent-primary);
            background-color: rgba(0, 0, 0, 0.5);
            box-shadow: 0 0 0 4px rgba(124, 77, 255, 0.2);
            transform: scale(1.02);
        }

        .btn-update {
            background: linear-gradient(135deg, var(--accent-primary), var(--accent-secondary));
            color: #fff;
            font-weight: 800;
            border: none;
            padding: 16px;
            border-radius: 14px;
            width: 100%;
            transition: all 0.3s cubic-bezier(0.175, 0.885, 0.32, 1.275);
            text-transform: uppercase;
            letter-spacing: 1px;
        }

        .btn-update:hover {
            transform: translateY(-5px) scale(1.02);
            box-shadow: 0 15px 30px rgba(124, 77, 255, 0.4);
            color: #fff;
        }

        .back-link {
            display: block;
            text-align: center;
            margin-top: 20px;
            color: var(--text-muted);
            text-decoration: none;
            font-size: 0.85rem;
            font-weight: 600;
            transition: 0.3s;
        }

        .back-link:hover {
            color: var(--accent-secondary);
            transform: translateY(-2px);
        }

        @media (max-width: 576px) {
            .form-container {
                border-radius: 20px;
                padding: 25px;
            }
        }
    </style>
</head>
<body>

    <div class="container d-flex justify-content-center">
        <div class="form-container">
            <h3>Edit Payload</h3>

            <form action="/chats/<%= chat._id %>?_method=PUT" method="POST">
                
                <div style="animation: fadeIn 0.5s ease-out 0.2s both;">
                    <label class="label-text">Initiator</label>
                    <input type="text" value="<%= chat.from %>" class="input-readonly" readonly>
                </div>

                <div style="animation: fadeIn 0.5s ease-out 0.3s both;">
                    <label class="label-text">Target</label>
                    <input type="text" value="<%= chat.to %>" class="input-readonly" readonly>
                </div>

                <div style="animation: fadeIn 0.5s ease-out 0.4s both;">
                    <label class="label-text">Data Update</label>
                    <textarea name="msg" class="custom-textarea" rows="4" placeholder="Update payload..." required><%= chat.msg %></textarea>
                </div>

                <div style="animation: fadeIn 0.5s ease-out 0.5s both;">
                    <button type="submit" class="btn-update">Deploy Update</button>
                    <a href="/chats" class="back-link">Return to Terminal</a>
                </div>
            </form>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>